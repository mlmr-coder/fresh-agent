# 远程服务只共享受管知识集

> 本决策的安全论证扩展自 ADR-0009（隔离远程上传与服务器本地导入）。

共享知识库服务只提供可以被 Agent 挂载和检索的受管知识集，不把主机整块磁盘或任意目录暴露为远程文件搜索。成员只能通过上传文件内容向知识集增加源文档，不能提交主机路径；具有管理访问的成员可在 Pinvou 统一“知识库”页面的“共享知识库”区域创建知识集、上传、更新和删除，只读成员不能改变内容。所有者也在 Pinvou 原生界面执行内容管理或主机本地导入。无论入口为何，进入知识集的源文档都遵循相同的解析、索引、权限和生命周期规则，主机上未导入的其他文件始终不可见。
