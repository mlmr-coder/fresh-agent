# 远程知识删除采用限期回收站

从共享知识库删除源文档或整个知识集时，服务先执行逻辑删除：对象立即从浏览、挂载和检索结果中消失，并进入回收站，默认保留 30 天后才自动物理清除。具有管理访问的成员可以查看和恢复回收站内容，但不能绕过回收期执行永久删除；只有所有者可以在 Pinvou 原生管理面提前永久清除，或修改全局保留天数。恢复应保留原知识集归属和稳定身份，并复用仍然有效的索引；索引不可用时进入重建流程，在完整就绪前不得以残缺结果对外检索。回收站用于防止内容维护者误删，不替代主机备份，也不保存普通文档更新的历史版本。
