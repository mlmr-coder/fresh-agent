# 远程检索不降级为纯关键词模式

远程知识库把 embedding 模型及完整的全文、语义索引视为对外检索的就绪条件，不在模型缺失、损坏或首次下载未完成时降级为纯关键词服务。此时服务器进程和 Web 管理后台仍可运行，管理员可以完成诊断、模型配置和文档上传；新文档进入等待队列，但尚未就绪的知识集不能被 Pinvou 挂载或检索。模型可用后，服务器自动处理队列并建立完整索引，只有索引成功的文档才进入当前可检索快照。这样可以保持远程知识库的检索语义稳定，并避免客户端误把临时降级结果当成完整知识。
