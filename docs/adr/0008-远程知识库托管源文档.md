# 远程知识库托管源文档

远程知识库不只保存解析文本、分块或向量，也将用户上传的源文档复制到服务管理的存储中，并以该副本作为远程知识集的数据来源。相比仅引用客户端或服务器外部路径，这会增加存储、备份和删除治理成本，但能在上传设备离线后继续预览和下载来源，也能在解析器、切块规则或 embedding 模型变化时可靠地重建索引。首期采用快照语义：上传设备上的原文件后续变化不会被常驻监听或自动同步，用户通过显式更新改变远程副本。更新只保留最新版，并先在临时版本完成解析和索引，再原子切换当前版本；更新失败时旧源文档及其索引继续可用。
