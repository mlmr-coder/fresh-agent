# 会话允许混合挂载本地与远程知识集

Pinvou 会话不在“本地模式”和“远程模式”之间二选一，而是允许同时挂载本地知识集与远程知识集。一次检索会并行查询所有已挂载且可用的知识集，再将结果合并为统一的候选来源；每条结果必须保留其所属知识库、知识集和源文档身份，以便正确打开来源且不混淆同名文档。远程知识库超时、断开或拒绝访问时，该来源应显示明确的局部错误，但不得阻断本地知识和其他可用远程来源的检索。首期不承诺离线缓存远程内容，服务器不可达时远程结果不可用。
