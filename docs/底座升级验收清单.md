# 底座升级验收清单(CodeWhale sync gate)

> **每次升级底座(sync 上游新 release)必须全过本清单才算完成。** 这是硬 gate,不是建议。
> 配套:`docs/fork-policy.md`(sync 流程/策略)、`docs/fork-modifications.md`(fork 现状清单)、`scripts/fork-guard.sh`。
> 来源:v0.8.60 sync(2026-06-15)实测固化——每一条都对应踩过的坑,删任何一条前先看「防什么」。
> 2026-08-22 修订:原文引用的三个 v0.8 时代 forkguard 测试名(`pinvou3_yolo_offers_nonblocklisted_tools_*`、
> `forkguard_yolo_no_deferred_activator_first_class`、`forkguard_blocklist_golden`)已随 0.9.5 clean re-fork 退役,
> 本版已按现行 `PINVOU3_ALLOWED_TOOLS` 机制改写;历史防回归案例保留供溯源。
>
> 用法:升级时把下面 **L0→L3 + 签收表**逐项跑过,签收表全勾才合并 / 更新父仓 gitlink。

---

## L0 — 编译(必过,最先)

| # | 命令 | 通过标准 |
|---|---|---|
| 0.1 | `cargo check -p codewhale-tui --lib --bins` | 0 error |
| 0.2 | `cargo check -p codewhale-tui --lib --tests` | 0 error(测试代码也编译——上游新增测试常构造 pinvou3 扩展了字段的 struct,缺字段编译错) |
| 0.3 | `cargo check --manifest-path pinvou3-app/src-tauri/Cargo.toml --lib` | 0 error(app bridge 依赖 submodule API,暴露 EngineConfig/Op 新字段) |
| 0.4 | `cargo run ... --bin dump_system_prompt`(能跑出非空) | exit 0(dump bin 随 PromptSessionContext 字段变化,常先编译错) |

> **防什么**:merge 后最常见的是 struct 加字段 / API 改签名,编译能抓绝大部分。`--tests` 和 app crate 单独 check 不能省(v0.8.60 的 sidebar.rs 测试、bridge 5 新字段、dump bin 2 新字段都是这层抓的)。

---

## L1 — 自动化测试(必过)

| # | 命令 | 通过标准 |
|---|---|---|
| 1.1 fork-guard 全套 | `bash scripts/fork-guard.sh` | ✅ 全过(指纹层 + 行为回归;指纹清单以脚本为准) |
| 1.2 全量 lib 测试 | `cargo test -p codewhale-tui --lib 2>&1 \| grep -E "FAILED\|test result"` | 0 failed(已知 flake `verifier 后台 shell 并行` 单跑复核即可,非回归) |
| 1.3 app lib 测试 | `cargo test --manifest-path pinvou3-app/src-tauri/Cargo.toml --lib -- --test-threads=1` | 0 failed(**必须单线程**,bridge env 测试并行会 flake) |
| 1.4 dump 前后 diff | sync 前存 baseline,sync 后 `diff /tmp/pre-sync-prompt.txt /tmp/post-sync-prompt.txt` | **逐字节一致(diff=空)** |
| 1.5 L1 真实对话 harness | `PINVOU3_L1_REQUIRE_VLLM=1 cargo test --manifest-path pinvou3-app/src-tauri/Cargo.toml --test l1_dialog_harness -- --ignored --test-threads=1` | `cargo test ... -- --list` 列出的场景全过；端点中途掉线必须失败，不能静默 skip(需 vLLM 在线,耗时随场景数增长) |

> **防什么**:
> - 1.2 抓「非 forkguard 前缀的上游测试因 fork fail」(v0.8.51 append_file 静默丢失靠此)。
> - 1.4 是 **prompt 字节稳定的唯一可靠抓手**——非 0 diff = 上游新静态块漏进 pinvou3 prompt(v0.8.57 Runtime Policy 141 行泄漏靠此抓)。dump bin 不在 fork-guard 构建里,必须单跑。
> - 1.5 是真 vLLM 行为回归,L0/L1 编译+单测抓不到的「模型实际行为变化」靠它。

---

## L2 — 功能验收(5 大功能区,逐项确认)

> L1.5 harness 已覆盖①③⑤⑥的大部分;②④需补 probe 或看通路。每项给「怎么验 + 通过标准」。

| # | 功能区 | 怎么验 | 通过标准 |
|---|---|---|---|
| 2.1 | **session 真实对话** | L1.5 harness 各 scenario(文件/搜索/exec/多turn/plan/长文/中文/视觉/错误恢复) | 全过;transcript 里工具链合理 |
| 2.2 | **卡牌池 persona 注入** | 取真实内置卡 → `personas::equip_body_injection` → `build_send_message_op` | persona 人设进 per-turn `<system-reminder>`;未加持时不注入。L2 `build_send_message_op_injects_persona_reminder_when_present` 守此 |
| 2.3 | **工具商店** | `SkillMarketplaceManager::list_skills` + 会话 `skills_dir` 物化通路；用户 Skills 能列出 | 列表不 crash、返回正确；LLM 会话挂载目录数量正常 |
| 2.4 | **system prompt (pinvou fork)** | = L1.4 dump diff | 逐字节一致;C7 composer 三道 gate(ContextMgmt/COMPACT/Runtime-Policy)+ runtime_prompt tag gate 在 |
| 2.5 | **tools 白名单** | 对照 `pinvou3-app/src-tauri/src/features/assistant/tool_policy.rs` 的 `PINVOU3_ALLOWED_TOOLS`(canonical 工具家族 + `mcp_*` 通配,共 18 条规则)+ L1.5 transcript 抽查 | 模型只调白名单内工具,**无裸 JSON / deferred 工具泄漏**;白名单变更须走 code review 显式登记 |

---

## L3 — 回归专项(自动化抓不到,每次必手查)

> 这些是 dump/编译/单测**都抓不到**的隐形回归,上游每个大版本都可能引入新的一种。每次 sync 必逐条过。

| # | 查什么 | 怎么查 | 防什么(实例) |
|---|---|---|---|
| 3.1 | **hook 决策协议 + execpolicy 安全规则集** | 读 `turn_loop.rs::fold_tool_call_before_results`,确认 deny 脚本(`deny_sensitive_paths.sh`)退出码契约仍成立(现仅第 5 段连接器自省纠正);再确认 `bridge.rs::scope_deny_ruleset` 的安全兜底规则(原敏感目录/危险命令/sudo 硬墙,已迁 `safety_deny_rules`)仍注入 `EngineConfig.exec_policy_engine` | **v0.8.60 Hooks v2** 把 hard-deny 从「非零」改成只认 `exit_code==2`,旧 `exit 1` 静默失效=自省纠正失灵;execpolicy 注入通道被上游改动绕过=敏感数据/sudo 硬墙再次失效 |
| 3.2 | **deferred 工具激活与 Hidden 隔离**(结果式守护) | 底座侧跑 `model_tool_catalog_defers_non_core_native_tools_in_yolo_mode`、`regex_tool_search_does_not_discover_hidden_exec_shell_alias`、`tool_search_activates_discovered_deferred_tools`（均在 `crates/tui/src/core/engine/tests.rs`）与宿主额外工具回归 `forkguard_host_extra_tools_register_in_all_modes`：断言首轮只有核心工具与唯一 `tool_search` 入口，普通 native/MCP 为 deferred，Hidden 工具既不能被搜索发现也不得经 unavailable fallback 泄漏，搜索仅激活命中的授权工具；辅以 grep 上游新增 `ensure_advanced_tooling`/preflight 类注入路径，并对照父仓 `tool_policy.rs` 的 `PINVOU3_ALLOWED_TOOLS` 与 `model_visible()` 别名机制（隐藏别名不得回流模型目录） | **v0.8.57 tool_search** 击穿 blocklist；**v0.8.65 折叠单名**致门控失效（改名非新增 + 断言查错前缀，间接检查三盲区全穿）——防止上游 activator 改名、硬注入或 fallback 新增路径后绕过三态工具面；单靠 ToolSpec 名单差集抓不到动态注入和兜底泄漏 |
| 3.3 | **per-turn 注入** | `grep -rn "runtime_prompt\|messages.push" turn_loop.rs engine.rs`;新增的 transient 注入要 gate | dump 只抓 system prompt,抓不到每请求注入的 transient 消息(runtime_prompt tag / goal continuation) |
| 3.4 | **工具集合盘点**(结果式守护) | 双侧盘点：底座侧 Hidden 名单靠 3.2 的目录/搜索行为回归锁定；父仓侧主查 `features/assistant/tool_policy.rs` 的 `PINVOU3_ALLOWED_TOOLS` 精确名单（增删即显式 diff，code review 逐项确认；工具塑造行为由 `shape_disallowed_tools_follows_session_policy` 等用例覆盖）；辅以目录一致性测试和新旧 `ToolSpec::name()` 集合差集 | 上游新增 model-facing 工具默认进入 Deferred，要确认应升为 Direct、降为 Hidden 或补父仓白名单；**动态注入/硬注入类工具不在 ToolSpec 名单里，对比 name 集抓不到，靠 3.2 的注入路径盘点** |
| 3.5 | **lib.rs 模块同步** | `git diff <old>..<new> --diff-filter=AD -- crates/tui/src/` 看模块增删 → 同步 `pub mod` | 上游加模块漏 `pub mod`=app 用不到;删模块留孤儿 `pub mod`=编译错(v0.8.60 `prompt_persist` 删除) |
| 3.6 | **EngineConfig/Op/PromptSessionContext 新字段语义** | 编译会逼你填,但要**判断 GUI 该填什么值**(透传 default 还是定制) | v0.8.60 verbosity/goal_*/interactive_launch_limit 等 GUI 透传 default;漏判会拿到错误默认行为 |
| 3.7 | **同名 API 语义分叉** | 上游新实现的同名函数/字段,**逐字段比对语义**(非冲突也非干净 harvest) | v0.8.57/60 composer `effective_static_prompt_composer` vs pinvou3 `static_prompt_composer`——同语义不同名,merge 取上游名要对齐 |
| 3.8 | **auto-compact 触发不倒置**(结果式) | 跑 `forkguard_compaction_threshold_below_emergency_all_windows`(pinvou3 T 换算**对拍底座 `context_input_budget_for_route` 的 E**,跨仓一致)+ `forkguard_compaction_128k_scenarios`;确认 128K/24K 得 `T=45648`、256K/24K 得 `T=133029`，且 route 同时携带 context/output limits | pinvou3 曾**镜像**底座 500K/262144/output 预留/`E=W−O−1024`,上游改这些 pinvou3 编译不报错却**静默倒置**。v0.9 对未知 wire alias 的静态 4K output fallback 还会把 T 错抬高；现在由 SavedModel route profile 提供部署事实，derive 直接调用底座 budget，显式 output 优先于模型名兼容 fallback |

---

## 收尾(验收过后)

- [ ] `fork-modifications.md` §0 当前状态更新(新增本次 sync 小节、drift/指纹/PR 状态)
- [ ] harvest 的 patch:撤 fork-guard 指纹 + §1 对应主题更新;新主题按 §5 后续修改规则登记
- [ ] `fork-policy.md` §0 基线更新(发布边界变化时同步 §6)
- [ ] submodule 在 `pinvou3-clean` 提交;drift 涨太多/历史乱 → 考虑 clean re-fork(fork-policy §4.2)
- [ ] 父仓更新 submodule gitlink + 提交
- [ ] push(force-push fork `pinvou3-clean` 前先推 backup 分支)

---

## ✅ 升级签收表(全勾才算完成)

```
版本: v____ → v____         日期: 20__-__-__       签收: ____

L0 编译
  [ ] 0.1 lib+bins   [ ] 0.2 lib+tests   [ ] 0.3 app lib   [ ] 0.4 dump bin

L1 自动化测试
  [ ] 1.1 fork-guard 全过(__ 指纹)
  [ ] 1.2 全量 lib 测试 0 failed
  [ ] 1.3 app lib 单线程 0 failed
  [ ] 1.4 dump 前后 diff = 空(字节稳定)
  [ ] 1.5 L1 harness __/__ 过

L2 功能验收
  [ ] 2.1 对话   [ ] 2.2 卡牌池   [ ] 2.3 工具商店
  [ ] 2.4 system prompt   [ ] 2.5 工具白名单

L3 回归专项
  [ ] 3.1 hook 协议   [ ] 3.2 deferred 激活   [ ] 3.3 per-turn 注入
  [ ] 3.4 工具集合   [ ] 3.5 lib 模块   [ ] 3.6 新字段语义   [ ] 3.7 同名 API
  [ ] 3.8 auto-compact

收尾
  [ ] 文档更新   [ ] submodule 提交   [ ] 父仓 gitlink   [ ] push
```
